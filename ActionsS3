

## boto3 
## can achieve by pip install boto3
## lets perform the [upload ,download] files and [create,Delete,List] of buckets 
#--------------- Ravi Rocky Ravindra -----#


import boto3 as rockye
Accesskeyinput("enter your access key here ... ")
SecretAccesskey=input("enter your secret key ... ")
session=rockye.Session(aws_access_key_id=Accesskeyinput,aws_secret_access_key=SecretAccesskey)

client=session.client(service_name="s3")

bucketslist=rockye.list_buckets()
allreg=client_describe_regions()
list_of_regions=[]

for listregions in allreg['Regions']:
    list_of_regions.append(listregions['RegionName'])  
    print(list_of_regions)
    
for each_region in list_of_regions:
    session=rockye.Session(profile_name="aws_root",region_name=each_region)
    resource=session.resource("s3")
    print("list of buckets in each region"each_region)
    for each_in in rocky.list_buckets():
    print(each_in.name)
    




